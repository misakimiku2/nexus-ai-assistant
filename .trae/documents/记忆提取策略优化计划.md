# 记忆提取策略优化计划（完整版）

## 问题分析

当前记忆提取产生 15\~30 条候选记忆，导致：

* 噪音过多

* 向量库污染

* 后续去重/合并成本急剧上升

**根本原因**：

1. **仅依赖 prompt 限制数量不可靠** - LLM 输出不稳定
2. **没有系统级硬限制** - 缺少代码层强制约束
3. **importance 阈值过低** - 0.6 仍会引入噪音
4. **没有内容去重保护** - LLM 可能输出语义重复内容
5. **importance 作为唯一排序依据** - LLM importance 不稳定
6. **没有类型均衡** - 可能某类过多，其他类缺失
7. **语义去重中文失效** - split(/\s+/) 在中文中无法分词

## 目标

让提取结果在进入阶段四前已经"**高度去重 + 结构稳定**"。

**核心指标**：

* 每次提取 **稳定 ≤ 8 条（无波动）**

* 平均 importance ≥ 0.75

* 无语义重复（中文兼容）

* 无短内容垃圾

* 类型分布均衡

## 执行顺序（Pipeline）

```
原始 candidates
    ↓
① importance 过滤（≥ 0.7）
    ↓
② 长度过滤（≥ 10 字符）
    ↓
③ 字符去重（normalize）
    ↓
④ 语义去重（n-gram similarity > 0.75）
    ↓
⑤ 排序（importance × 0.7 + lengthScore × 0.3）
    ↓
⑥ 类型均衡（每类上限）
    ↓
⑦ 截断 ≤ 8
```

## 实施步骤

### 步骤 1：修改前端提取 Prompt

**文件**：`src/agent/memory/MemoryExtractionService.ts`

修改 `callLLMForExtraction` 方法中的 `systemPrompt`：

```
【提取原则】
只提取"最重要、最稳定、可复用"的信息。

【忽略内容】
- 临时讨论内容
- 重复表达
- 细节举例
- 枚举类信息（如一堆角色名、物品列表）

【优先提取】
- 用户长期偏好
- 稳定事实
- 关键能力/约束

【数量限制】
- identity: ≤2 条
- facts: ≤3 条
- preferences: ≤3 条
- tasks: ≤2 条
- constraints: ≤2 条
- skills: ≤2 条
- 总计 ≤8 条

【强制要求】
如果提取结果超过 8 条，请只保留最重要的 8 条，其余丢弃。

【合并规则】
禁止拆分细粒度事实，应合并为一条：
❌ 错误："今汐有叠层机制"、"守岸人能回血"、"维里奈能闪避"
✅ 正确："游戏包含多种角色机制（叠层爆发、护盾、闪避等）"
```

### 步骤 2：完整 Pipeline 实现

**文件**：`src/agent/memory/MemoryExtractionService.ts`

```typescript
private convertToCandidates(
  extracted: any,
  sessionId: string,
  messages: ConversationMessage[]
): CandidateMemory[] {
  const messageIds = messages.map(m => m.id || crypto.randomUUID());

  // 统计变量
  const stats = {
    原始数量: 0,
    importance过滤: 0,
    长度过滤: 0,
    字符去重前: 0,
    字符去重后: 0,
    语义去重后: 0,
    类型均衡后: 0,
    最终数量: 0,
  };

  // ========== Step 0: 收集所有候选 ==========
  const rawCandidates: CandidateMemory[] = [];

  const processItems = (items: any[], memoryType: string) => {
    for (const item of items) {
      stats.原始数量++;
      rawCandidates.push({
        id: crypto.randomUUID(),
        content: item.content?.trim() || '',
        memoryType: memoryType as any,
        confidence: 0.5,
        sourceSessionId: sessionId,
        sourceMessageIds: messageIds,
        createdAt: Date.now(),
        status: 'pending',
        importance: typeof item.importance === 'number' ? item.importance : 0.5,
      });
    }
  };

  if (Array.isArray(extracted.identity)) processItems(extracted.identity, 'identity');
  if (Array.isArray(extracted.facts)) processItems(extracted.facts, 'fact');
  if (Array.isArray(extracted.preferences)) processItems(extracted.preferences, 'preference');
  if (Array.isArray(extracted.tasks)) processItems(extracted.tasks, 'task');
  if (Array.isArray(extracted.constraints)) processItems(extracted.constraints, 'constraint');
  if (Array.isArray(extracted.skills)) processItems(extracted.skills, 'skill');

  // ========== Step 1: importance 过滤（≥ 0.7）==========
  let candidates = rawCandidates.filter(c => {
    if (c.importance < 0.7) {
      stats.importance过滤++;
      return false;
    }
    return true;
  });

  // ========== Step 2: 长度过滤（≥ 10 字符）==========
  candidates = candidates.filter(c => {
    if (c.content.length < 10) {
      stats.长度过滤++;
      return false;
    }
    return true;
  });

  // ========== Step 3: 字符去重（normalize）==========
  stats.字符去重前 = candidates.length;
  const charUnique = new Map<string, CandidateMemory>();
  for (const c of candidates) {
    const key = this.normalizeContent(c.content);
    if (!charUnique.has(key)) {
      charUnique.set(key, c);
    }
  }
  candidates = Array.from(charUnique.values());
  stats.字符去重后 = candidates.length;

  // ========== Step 4: 语义去重（n-gram similarity > 0.75）==========
  candidates = this.semanticDedup(candidates);
  stats.语义去重后 = candidates.length;

  // ========== Step 5: 排序（importance × 0.7 + lengthScore × 0.3）==========
  candidates.sort((a, b) => this.calculateScore(b) - this.calculateScore(a));

  // ========== Step 6: 类型均衡 ==========
  candidates = this.balanceTypes(candidates);
  stats.类型均衡后 = candidates.length;

  // ========== Step 7: 截断 ≤ 8 ==========
  const finalCandidates = candidates.slice(0, 8);
  stats.最终数量 = finalCandidates.length;

  // 质量统计日志
  const avgImportance = finalCandidates.length > 0
    ? (finalCandidates.reduce((sum, c) => sum + c.importance, 0) / finalCandidates.length).toFixed(2)
    : '0.00';

  console.log('[MemoryExtraction] 质量统计:', {
    ...stats,
    平均importance: avgImportance,
  });

  return finalCandidates;
}

// normalize 函数：字符级去重
private normalizeContent(content: string): string {
  return content
    .toLowerCase()
    .replace(/[^\u4e00-\u9fa5a-z0-9]/g, '')
    .trim();
}

// 语义去重：n-gram 相似度 > 0.75 视为重复
private semanticDedup(candidates: CandidateMemory[]): CandidateMemory[] {
  const result: CandidateMemory[] = [];

  for (const c of candidates) {
    const isDuplicate = result.some(existing =>
      this.calculateSimilarity(existing.content, c.content) > 0.75
    );

    if (!isDuplicate) {
      result.push(c);
    }
  }

  return result;
}

// 相似度计算（字符 n-gram，中文兼容）
private calculateSimilarity(a: string, b: string): number {
  const getNgrams = (text: string, n = 2): Set<string> => {
    const grams = new Set<string>();
    const cleaned = text.toLowerCase().replace(/\s+/g, '');
    for (let i = 0; i < cleaned.length - n + 1; i++) {
      grams.add(cleaned.slice(i, i + n));
    }
    return grams;
  };

  const gramsA = getNgrams(a);
  const gramsB = getNgrams(b);

  if (gramsA.size === 0 || gramsB.size === 0) return 0;

  const intersection = new Set([...gramsA].filter(x => gramsB.has(x)));
  const union = new Set([...gramsA, ...gramsB]);

  return intersection.size / union.size;
}

// 综合评分：importance × 0.7 + lengthScore × 0.3
private calculateScore(c: CandidateMemory): number {
  const lengthScore = Math.min(c.content.length / 50, 1);
  return c.importance * 0.7 + lengthScore * 0.3;
}

// 类型均衡：每类有上限
private balanceTypes(candidates: CandidateMemory[]): CandidateMemory[] {
  const limits: Record<string, number> = {
    identity: 2,
    fact: 3,
    preference: 3,
    task: 2,
    constraint: 2,
    skill: 2,
  };

  const result: CandidateMemory[] = [];
  const counts: Record<string, number> = {};

  for (const c of candidates) {
    const type = c.memoryType;
    counts[type] = counts[type] || 0;

    if (counts[type] < (limits[type] || 2)) {
      result.push(c);
      counts[type]++;
    }

    if (result.length >= 8) break;
  }

  return result;
}
```

### 步骤 3：同步修改 Rust 后端 Prompt

**文件**：`src-tauri/src/memory/extraction.rs`

修改 `COGNITIVE_MEMORY_EXTRACTION_PROMPT` 常量，与前端 prompt 保持一致。

## 关键修复：中文语义去重

### 问题

原方案使用 `.split(/\s+/)` 分词，在中文文本中：

* 整句会被视为一个 token

* 相似度计算几乎恒为 0

* 导致语义去重失效

### 解决方案

改用**字符 n-gram** 方式（无需引入分词库）：

```typescript
private calculateSimilarity(a: string, b: string): number {
  const getNgrams = (text: string, n = 2): Set<string> => {
    const grams = new Set<string>();
    const cleaned = text.toLowerCase().replace(/\s+/g, '');
    for (let i = 0; i < cleaned.length - n + 1; i++) {
      grams.add(cleaned.slice(i, i + n));
    }
    return grams;
  };

  const gramsA = getNgrams(a);
  const gramsB = getNgrams(b);

  if (gramsA.size === 0 || gramsB.size === 0) return 0;

  const intersection = new Set([...gramsA].filter(x => gramsB.has(x)));
  const union = new Set([...gramsA, ...gramsB]);

  return intersection.size / union.size;
}
```

### 示例

```
"用户喜欢玩游戏" → n-grams: {"用户", "户喜", "喜欢", "欢玩", "玩游戏"}
"用户喜欢看电影" → n-grams: {"用户", "户喜", "喜欢", "欢看", "看电影"}

交集: {"用户", "户喜", "喜欢"} = 3
并集: 5 + 5 - 3 = 7
相似度: 3/7 ≈ 0.43
```

### 阈值

* 当前阈值：**0.75**（可根据测试微调）

* 目标：在进入向量检索前，过滤掉明显重复

## 验收标准

* [ ] 每次提取 **稳定 ≤ 8 条（无波动）**

* [ ] 平均 importance ≥ 0.75

* [ ] 中文相似句可正确识别为重复

* [ ] 语义去重后数量明显下降

* [ ] 无短内容垃圾（< 10 字符）

* [ ] 类型分布均衡（每类不超限）

* [ ] 日志清晰显示完整 Pipeline 统计

* [ ] 阶段四 merge 调用减少

## 文件修改清单

| 文件                                            | 修改内容                                              |
| --------------------------------------------- | ------------------------------------------------- |
| `src/agent/memory/MemoryExtractionService.ts` | 修改 systemPrompt、完整 Pipeline 实现（7 步）、中文 n-gram 相似度 |
| `src-tauri/src/memory/extraction.rs`          | 同步修改 COGNITIVE\_MEMORY\_EXTRACTION\_PROMPT        |

## 风险评估

* **低风险**：仅修改提取逻辑和过滤规则，不影响现有架构

* **可回滚**：阈值参数可调整

* **可观测**：日志清晰显示每个 Pipeline 步骤的效果

* **轻量级**：n-gram 方案无需引入分词库，性能开销小

