# 记忆提取策略优化实施记录

## 会话日期
2026-03-26

## 问题背景

当前记忆提取系统存在以下问题：
1. **过度提取**：每次提取产生 15~30 条候选记忆
2. **噪音过多**：低质量记忆污染向量库
3. **增量提取缺失**：每次从全量历史重新提取
4. **JSON 解析脆弱**：LLM 输出稍有偏差即解析失败
5. **Qwen 3.5 thinking 模式**：模型把内容放在 `reasoning_content` 中
6. **Embedding 未被实际使用**：已集成 bge-small-zh-v1.5 但未在流程中使用
7. **缺乏语义去重**：相似记忆不断重复写入
8. **缺乏决策层**：完全依赖 LLM 输出决定记忆写入

## 目标

将 Memory System 从"生成驱动"升级为"**规则 + 语义驱动**"，并确保：
* **长期稳定可控**
* **Embedding 高效复用**
* **Merge 策略保守且安全**
* **状态隔离无污染**
* **生产环境可靠**

---

## 系统架构

```
Conversation（原始对话）
    ↓
┌─────────────────────────────────┐
│  PreFilter 层                   │
│  - 过滤短消息 (< 20 字符)        │
│  - 过滤工具调用消息              │
│  - 截取最近 N 条消息             │
│  - 仅做完全字符串去重            │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Memory Extraction Model        │
│  - Qwen2.5-3B-Instruct          │
│  - temperature=0.2              │
│  - max_tokens=800               │
│  - 输出半结构化文本              │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Multi-Pass Parser              │
│  - Pass 1: 严格正则匹配          │
│  - Pass 2: 宽松模式匹配          │
│  - Pass 3: 关键词提取兜底        │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  SafeExtract 防护层             │
│  - 每轮 reset 状态               │
│  - 内容长度校验 (10-200 字符)    │
│  - 类型白名单校验                │
│  - importance 范围校验           │
│  - 完全字符串去重                │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Embedding 层（并行批量计算）    │
│  - bge-small-zh-v1.5            │
│  - Promise.all 并行生成          │
│  - 向量缓存在 ParsedMemory 中    │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Similarity Engine（核心决策层） │
│  - 复用已计算的 embedding        │
│  - 动态 topK（基于总量）         │
│  - 先全局搜索，再类型过滤        │
│  - 类型保护：duplicate 必须同类型│
│  - cosine similarity 计算       │
│  - >0.85 && 同类型 → SKIP       │
│  - 0.75~0.85 → MERGE（带fallback）│
│  - <0.75 → INSERT               │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Memory Score 计算（接入系统）   │
│  - score = importance × decay   │
│  - insert 时写入 score          │
│  - 清理策略：score < 0.3 删除   │
└─────────────────────────────────┘
    ↓
Memory Store
```

---

## 🚨 生产环境关键问题修复

### 问题一：Merge 失败导致数据丢失

**问题描述**：
```typescript
// A: 用户使用 React
// B: 用户使用 Vue
// 相似度 0.78 → 进入 merge
// conservativeMerge 返回 existing（React）
// → Vue 信息直接丢失！
```

**解决方案**：Merge Fallback 机制
```typescript
case 'merge':
  const merged = await mergeMemories(existing, candidate)
  
  if (merged === existing.content) {
    // ❗合并失败，fallback 到 insert
    const memory = await insertMemory(candidate)
    result.accepted.push(memory)
  } else {
    result.merged.push(...)
  }
  break
```

**原则**：merge 失败 ≠ 可以丢数据

---

### 问题二：类型错杀问题

**问题描述**：
```typescript
// [preference] 用户喜欢猫
// [fact] 用户养了一只猫
// embedding 相似度 0.87+
// → 被误判为 duplicate
```

**解决方案**：Duplicate 判断加类型保护
```typescript
if (similarity > duplicateThreshold && type相同) {
  skip
}
```

---

### 问题三：伪批量 Embedding 生成

**问题描述**：
```typescript
// 串行 batch（假批量）
for (const candidate of candidates) {
  candidate.embedding = await generateEmbedding()  // 逐个等待
}
```

**解决方案**：真正的并行批量
```typescript
await Promise.all(
  candidates.map(async (c) => {
    if (!c.embedding) {
      c.embedding = await generateEmbedding(c.content)
    }
  })
)
```

---

### 问题四：Memory Score 未接入系统

**问题描述**：
* 定义了 Score
* 计算了 Score
* 但没有任何地方使用它

**解决方案**：
1. Insert 时使用 Score
2. 清理策略：`if (score < 0.3) delete`

---

### 问题五：topK 限制问题

**问题描述**：
* 当 memory 变成 1000+ 条
* topK=10 可能找不到真正最相似的
* merge/skip 全部失效

**解决方案**：动态 topK
```typescript
const topK = Math.max(10, Math.floor(totalMemories * 0.05))
```

---

## 实施的修改

### 1. 类型定义与配置存储

**文件**: `src/types.ts`

**新增类型**:
```typescript
export interface MemoryModelConfig {
  enabled: boolean;
  provider: 'lm-studio' | 'ollama';
  baseUrl: string;
  modelName: string;
  temperature: number;
  maxTokens: number;
}

export const DEFAULT_MEMORY_MODEL_CONFIG: MemoryModelConfig = {
  enabled: true,
  provider: 'lm-studio',
  baseUrl: 'http://localhost:1234/v1',
  modelName: 'qwen2.5-3b-instruct',
  temperature: 0.2,
  maxTokens: 800,
};

export interface PreFilterConfig {
  minMessageLength: number;
  maxMessages: number;
  skipToolCalls: boolean;
}

export interface SimilarityDecision {
  action: 'skip' | 'merge' | 'insert';
  targetMemory?: MemoryItem;
  similarity: number;
  reason: string;
}

export interface SimilarityThresholds {
  duplicate: number;
  merge: number;
  minSimilarity: number;
  minScoreForCleanup: number;
}

export interface MemoryScore {
  base: number;
  decay: number;
  access: number;
  final: number;
}
```

### 2. PreFilter 层实现

**文件**: `src/agent/memory/PreFilterService.ts` (新增)

**功能**:
- 过滤短消息 (< 20 字符)
- 过滤工具调用消息
- 截取最近 N 条消息
- 仅做完全字符串去重

**核心方法**:
- `filter()`: 过滤消息
- `exactDeduplicate()`: 完全字符串去重
- `formatForExtraction()`: 格式化输出

### 3. Memory Model Client 实现

**文件**: `src/agent/memory/MemoryModelClient.ts` (新增)

**输出格式**:
```
[类型] 内容 | 重要性
```

**Multi-Pass Parser**:
- Pass 1: 严格正则匹配
- Pass 2: 宽松模式匹配
- Pass 3: 关键词提取兜底

**SafeExtract 防护层**:
- 每轮 reset 状态
- 内容长度校验 (10-200 字符)
- 类型白名单校验
- importance 范围校验 (0.7-1.0)
- 完全字符串去重

### 4. Similarity Engine 实现

**文件**: `src/agent/memory/SimilarityEngine.ts` (新增)

**关键特性**:
1. **动态 topK**: `Math.min(50, Math.max(10, Math.floor(totalMemories * 0.05)))`
2. **类型保护**: duplicate 判断必须同类型
3. **MergeResult**: 返回 `wasMerged` 标识是否真正合并

**决策逻辑**:
- similarity > 0.85 && 同类型 → SKIP
- 0.75 < similarity <= 0.85 → MERGE（带 fallback）
- similarity <= 0.75 → INSERT

### 5. Memory Score 计算

**文件**: `src/agent/memory/MemoryScoreCalculator.ts` (新增)

**计算公式**:
```typescript
score = importance × decay + access
```

其中:
- `decay = 0.5^(ageInDays / halfLifeDays)`，halfLifeDays = 30
- `access = min(accessCount * 0.01, 0.2)`

**清理策略**: `score < 0.3` 删除

### 6. Memory Store 集成

**文件**: `src/agent/memory/MemoryStore.ts` (新增)

**关键修复**:
1. **并行批量生成 embedding**: 使用 `Promise.all`
2. **Merge fallback 机制**: 合并失败时 fallback 到 insert
3. **Memory Score 接入**: insert 时写入 score

### 7. MemoryExtractionService 重构

**文件**: `src/agent/memory/MemoryExtractionService.ts`

**修改内容**:
- 整合所有新组件
- 添加 `cleanupLowScoreMemories()` 方法
- 增量提取机制
- 游标重置 `resetCursor()`

### 8. TauriMemoryClient 扩展

**文件**: `src/agent/memory/TauriMemoryClient.ts`

**新增方法**:
- `generateEmbedding()`: 生成 Embedding
- `searchSimilarMemories()`: 搜索相似记忆（全局 topK）
- `boostMemory()`: Boost 记忆重要性
- `updateMemory()`: 更新记忆内容
- `addMemory()`: 添加记忆
- `deleteMemory()`: 删除记忆
- `getAllMemories()`: 获取所有记忆
- `getMemoryStats()`: 获取记忆统计

### 9. 提取 Prompt 优化

**文件**: `src/agent/memory/MemoryExtractionService.ts`

**修改内容**:
- 添加提取原则：只提取"最重要、最稳定、可复用"的信息
- 添加忽略内容：临时讨论、重复表达、细节举例、枚举类信息
- 添加数量限制：每类 ≤2-3 条，总计 ≤8 条
- 添加合并规则：禁止拆分细粒度事实
- 添加上下文说明：告知 LLM 只提取新增信息

### 10. 完整 Pipeline 实现（7 步）

```
原始 candidates
    ↓
① importance 过滤（≥ 0.7）
    ↓
② 长度过滤（≥ 10 字符）
    ↓
③ 字符去重（normalize）
    ↓
④ 语义去重（n-gram similarity > 0.75）
    ↓
⑤ 排序（importance × 0.7 + lengthScore × 0.3）
    ↓
⑥ 类型均衡（每类上限）
    ↓
⑦ 截断 ≤ 8
```

**新增方法**:
- `normalizeContent()`: 字符级去重
- `semanticDedup()`: 语义去重
- `calculateSimilarity()`: 中文 n-gram 相似度计算
- `calculateScore()`: 综合评分
- `balanceTypes()`: 类型均衡

### 11. 增量提取机制

**新增属性**:
- `lastProcessedIndex`: 记录已处理的消息位置
- `processedContentHashes`: 防止重复提取相同内容

**新增方法**:
- `resetCursor()`: 重置游标

**触发条件**:
- 消息数 ≥ 4 或 内容长度 ≥ 300

### 12. Qwen 3.5 Thinking 模式兼容

**问题**: Qwen 3.5 默认启用 thinking 模式，内容放在 `reasoning_content` 中

**解决方案**:
1. 添加 `chat_template_kwargs: { enable_thinking: false }` 参数尝试禁用
2. 新增 `extractJsonFromReasoning()` 方法从 `reasoning_content` 中提取 JSON

### 13. JSON 解析增强

**问题**: 
- 模型可能输出多个 JSON 对象
- `lastIndexOf('}')` 会包含所有 JSON

**解决方案**:
改用括号匹配找到第一个完整的 JSON 对象：

```typescript
const startBrace = trimmed.indexOf('{');
if (startBrace !== -1) {
  let braceCount = 0;
  let endBrace = -1;
  
  for (let i = startBrace; i < trimmed.length; i++) {
    if (trimmed[i] === '{') braceCount++;
    if (trimmed[i] === '}') braceCount--;
    
    if (braceCount === 0) {
      endBrace = i;
      break;
    }
  }
  
  if (endBrace > startBrace) {
    return trimmed.substring(startBrace, endBrace + 1);
  }
}
```

### 14. 参数调整

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| `max_tokens` | 1500 | 4000 | 给模型足够的 token 输出完整的 JSON |
| `importance 阈值` | 0.6 | 0.7 | 提高记忆质量 |
| `最小内容长度` | 5 | 10 | 过滤短内容垃圾 |
| `触发消息数` | 2 | 4 | 减少频繁触发 |
| `触发内容长度` | 无 | 300 | 双阈值触发 |

### 15. Rust 后端同步

**文件**: `src-tauri/src/memory/extraction.rs`

**修改内容**: 同步修改 `COGNITIVE_MEMORY_EXTRACTION_PROMPT` 常量

### 16. 质量统计日志

**新增日志**:
```typescript
console.log('[MemoryExtraction] 质量统计:', {
  原始数量,
  importance过滤,
  长度过滤,
  字符去重前,
  字符去重后,
  语义去重后,
  类型均衡后,
  最终数量,
  平均importance,
});
```

### 17. 全局状态管理重构

**问题**: 
- App.tsx 中存在局部状态 `temperature`、`systemPrompt` 等，与 GlobalStateContext 中的状态不同步
- 导致模型名称、温度等设置无法正确传递到 API 调用
- 出现 `temperature is not defined` 运行时错误

**解决方案**:

#### 17.1 将聊天模型设置迁移到 GlobalStateContext

**文件**: `src/context/GlobalStateContext.tsx`

**新增状态**:
```typescript
// Chat Model Settings
const [lmStudioUrl, setLmStudioUrl] = useState<string>(() => {
  return localStorage.getItem('nexus_lm_studio_url') || 'http://localhost:1234/v1/chat/completions';
});
const [ollamaUrl, setOllamaUrl] = useState<string>(() => {
  return localStorage.getItem('nexus_ollama_url') || 'http://localhost:11434/api/chat';
});
const [modelName, setModelName] = useState<string>(() => {
  return localStorage.getItem('nexus_model_name') || '';
});
const [modelTemperature, setModelTemperature] = useState<number>(() => {
  const stored = localStorage.getItem('nexus_model_temperature');
  return stored ? parseFloat(stored) : 0.7;
});
const [maxContextLength, setMaxContextLength] = useState<number>(() => {
  const stored = localStorage.getItem('nexus_max_context_length');
  return stored ? parseInt(stored) : 4096;
});
const [modelProvider, setModelProvider] = useState<ModelProvider>(() => {
  return (localStorage.getItem('nexus_model_provider') as ModelProvider) || 'lm-studio';
});
const [systemPrompt, setSystemPrompt] = useState<string>(() => {
  return localStorage.getItem('nexus_system_prompt') || '';
});
```

**localStorage 持久化**:
```typescript
useEffect(() => { localStorage.setItem('nexus_lm_studio_url', lmStudioUrl); }, [lmStudioUrl]);
useEffect(() => { localStorage.setItem('nexus_ollama_url', ollamaUrl); }, [ollamaUrl]);
useEffect(() => { localStorage.setItem('nexus_model_name', modelName); }, [modelName]);
useEffect(() => { localStorage.setItem('nexus_model_temperature', String(modelTemperature)); }, [modelTemperature]);
useEffect(() => { localStorage.setItem('nexus_max_context_length', String(maxContextLength)); }, [maxContextLength]);
useEffect(() => { localStorage.setItem('nexus_model_provider', modelProvider); }, [modelProvider]);
useEffect(() => { localStorage.setItem('nexus_system_prompt', systemPrompt); }, [systemPrompt]);
```

#### 17.2 简化组件 Props

**文件**: `src/components/SettingsView.tsx`

**修改前**:
```typescript
interface SettingsViewProps {
  isOpen: boolean;
  onClose: () => void;
  isDarkMode: boolean;
  systemPrompt: string;
  setSystemPrompt: (prompt: string) => void;
  onReset: () => void;
  // ... 以及其他已移除的 props
}
```

**修改后**:
```typescript
interface SettingsViewProps {
  isOpen: boolean;
  onClose: () => void;
  isDarkMode: boolean;
  onReset: () => void;
}

// 组件内部通过 useGlobalState() 获取所有设置状态
const { 
  lmStudioUrl, setLmStudioUrl,
  ollamaUrl, setOllamaUrl,
  modelName, setModelName,
  modelTemperature, setModelTemperature,
  maxContextLength, setMaxContextLength,
  modelProvider, setModelProvider,
  systemPrompt, setSystemPrompt
} = useGlobalState();
```

**文件**: `src/components/ToolPanel.tsx`

**修改前**:
```typescript
interface ToolPanelProps {
  isOpen: boolean;
  onClose: () => void;
  isDarkMode: boolean;
  temperature: number;
  setTemperature: (temp: number) => void;
  systemPrompt: string;
  setSystemPrompt: (prompt: string) => void;
}
```

**修改后**:
```typescript
interface ToolPanelProps {
  isOpen: boolean;
  onClose: () => void;
  isDarkMode: boolean;
}

// 组件内部通过 useGlobalState() 获取状态
const { 
  modelTemperature,
  setModelTemperature,
  systemPrompt,
  setSystemPrompt
} = useGlobalState();
```

#### 17.3 修复 ModelProvider 类型

**文件**: `src/context/GlobalStateContext.tsx`

**问题**: `modelProvider` 类型不包含 `'online'` 选项，导致 TypeScript 编译错误

**解决方案**:
```typescript
// 从 types.ts 导入 ModelProvider 类型
import { ModelProvider } from '../types';

// 使用完整类型
modelProvider: ModelProvider;
setModelProvider: (provider: ModelProvider) => void;
```

#### 17.4 添加 systemPrompt 默认值处理

**文件**: `src/App.tsx`

**问题**: `systemPrompt` 为空时，API 调用会发送空字符串

**解决方案**:
```typescript
// 在调用 requestAI 时使用默认值
const promptToUse = systemPrompt || t.systemPrompts.defaultAssistant;
await requestAI(currentMsgs, promptToUse, originalInput);
```

---

## 文件修改清单

| 文件 | 操作 | 修改内容 |
|------|------|---------|
| `src/types.ts` | 修改 | 新增类型定义（MemoryModelConfig、PreFilterConfig、SimilarityDecision、SimilarityThresholds、MemoryScore） |
| `src/context/GlobalStateContext.tsx` | 修改 | 新增 `memoryModelConfig` 状态、聊天模型设置状态（lmStudioUrl、ollamaUrl、modelName、modelTemperature、maxContextLength、modelProvider、systemPrompt），添加 localStorage 持久化 |
| `src/agent/memory/PreFilterService.ts` | **新增** | PreFilter（仅完全去重） |
| `src/agent/memory/MemoryModelClient.ts` | **新增** | Memory Model + Parser + SafeExtract |
| `src/agent/memory/SimilarityEngine.ts` | **新增** | 核心决策层（动态topK + 类型保护 + MergeResult） |
| `src/agent/memory/MemoryScoreCalculator.ts` | **新增** | Memory Score（接入系统 + 清理功能） |
| `src/agent/memory/MemoryStore.ts` | **新增** | Memory Store（并行embedding + Merge fallback） |
| `src/agent/memory/MemoryExtractionService.ts` | 修改 | Prompt 优化、Pipeline 实现、增量提取、JSON 解析增强、参数调整、整合所有组件 + 清理功能 |
| `src/agent/memory/TauriMemoryClient.ts` | 修改 | 新增方法（generateEmbedding、searchSimilarMemories、boostMemory、updateMemory、addMemory、deleteMemory、getAllMemories、getMemoryStats） |
| `src/agent/memory/index.ts` | 修改 | 导出新模块 |
| `src/components/SettingsView.tsx` | 修改 | 新增 Memory Model 设置区块、简化 Props 接口，从 GlobalStateContext 获取设置状态 |
| `src/components/ToolPanel.tsx` | 修改 | 简化 Props 接口，从 GlobalStateContext 获取设置状态 |
| `src/App.tsx` | 修改 | 从 GlobalStateContext 获取设置状态，添加 systemPrompt 默认值处理 |
| `src-tauri/src/memory/extraction.rs` | 修改 | 同步修改 Prompt |
| `src/agent/memory/DefaultAgentLayer.ts` | 修改 | 修复 MemoryItem 缺少字段问题 |
| `src/i18n/locales/zh.json` | 修改 | 新增中文翻译 |
| `src/i18n/locales/en.json` | 修改 | 新增英文翻译 |

---

## 🚨 生产环境关键修复总结

| 问题 | 修复方案 |
|------|---------|
| Merge 失败丢数据 | MergeResult.wasMerged + fallback to insert |
| 类型错杀 | duplicate 判断必须同类型 |
| 伪批量 Embedding | Promise.all 并行生成 |
| Memory Score 未接入 | insert 时写入 score + 清理功能 |
| topK 限制 | 动态 topK = max(10, min(50, total × 0.05)) |
| 状态不同步 | 将聊天模型设置迁移到 GlobalStateContext |

---

## 数据流总结

```
原始消息 → PreFilter → Memory Model → Parser → SafeExtract（reset）
    → Embedding（Promise.all 并行）
    → Similarity（动态topK + 类型保护）
    → Merge（带 fallback）
    → Memory Score（写入 + 清理）
    → Store
```

---

## 验收标准

- [x] TypeScript 编译通过
- [x] Rust 编译通过
- [x] 应用可正常启动（无 `temperature is not defined` 错误）
- [x] 模型设置可正确保存到 localStorage
- [x] 模型设置在 SettingsView 和 ToolPanel 中可正确显示和修改
- [ ] PreFilter 仅做完全字符串去重
- [ ] SafeExtract 每轮 reset 状态
- [ ] Embedding 并行生成（Promise.all）
- [ ] Similarity 动态 topK
- [ ] Duplicate 必须同类型
- [ ] Merge 失败 fallback 到 insert
- [ ] Memory Score 写入并用于清理
- [ ] 设置界面正确保存配置
- [ ] 端到端提取流程正常工作
- [ ] 每次提取稳定 ≤ 8 条
- [ ] 平均 importance ≥ 0.75
- [ ] 中文相似句可正确识别为重复
- [ ] 无短内容垃圾
- [ ] 类型分布均衡
- [ ] 日志清晰显示完整 Pipeline 统计

---

## 后续工作

1. 测试验证提取效果
2. 根据测试结果微调阈值
3. 清理原有记忆数据库（可选）
4. 继续阶段四：记忆合并与演化
5. 测试验证全局状态管理是否正常工作
